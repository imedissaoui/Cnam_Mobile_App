import 'package:flutter/material.dart';
class ChercherAssure extends StatelessWidget {
  const ChercherAssure({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
